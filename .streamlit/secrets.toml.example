# Copy this file to .streamlit/secrets.toml and set a real password.
# .streamlit/secrets.toml is gitignored — never commit real secrets.
#
# On Streamlit Community Cloud, set this in your app's
# Settings -> Secrets instead of committing a file.

ADMIN_PASSWORD = "change-me"
