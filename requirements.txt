# === Django & DRF ===
django>=5.1,<6.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4

# === ML & Data ===
scikit-learn==1.5.2
pandas==2.2.3
joblib==1.4.2
numpy>=1.24

# === Testing ===
pytest>=8.0
pytest-django>=4.8

# === Dev Server (optional — Django's runserver is fine) ===
# uvicorn[standard]==0.32.1
# gunicorn>=21.2 (Linux production)

# === Legacy (keep for reference, not needed with Django) ===
# streamlit>=1.24
# fastapi==0.115.5